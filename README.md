# HarmoniQ

Music learning prototype (Next.js 14 + Tailwind). The app lives in **`harmoniq/`**.

```bash
cd harmoniq
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Or from this folder: `./start-harmoniq.sh`
